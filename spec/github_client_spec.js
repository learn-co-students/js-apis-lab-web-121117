describe('js-apis-lab', function() {
  var expectedData;
  var fakeGist;

  beforeEach(function() {
    expectedData = null;
    fakeGist = {
      owner: {
        login: 'fake login'
      }
    };

  });

"lame tests. this works."


});
